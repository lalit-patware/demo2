<td><%= post.title %></td>
<td><%= post.description %></td>
<td><%= image_tag post.image.url, width: "50px", height: "50px"  if post.image? %></td>
<p class = "links">
<td><%= link_to 'Show', post_path(post), class: "btn btn-primary btn-xs" %></td>
<td><%= link_to 'Edit', edit_post_path(post),remote: true, class: "btn btn-primary btn-xs" %></td>
<td><%= link_to 'Destroy', post_path(post),
        method: :delete,
        data: { confirm: 'delete permanently?' },remote: true, class: "btn btn-primary btn-xs" %></td>
</p>        

<!-- <div class="dropdown navbar-right">
  <button class="btn btn-primary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
  	<%#=post.title %>
  	<span class="caret"></span>
  </button>
  	<ul class="dropdown-menu">
    	<li><%#= link_to 'Show', post_path(post), class: "btn btn-primary btn-xs"  %> </li>
      <li><%#= link_to 'Edit', edit_post_path(post), class: "btn btn-primary btn-xs"  %></li>
      <li><%#= link_to "destroy", post_path(post), method: :delete,
        data: { confirm: 'delete permanently?' },remote: true, class: "btn btn-primary btn-xs"%>
       </li>
                
     </ul>
</div> -->